#include "main.h"
/**
 *  * _abs - computes the absolute value of an int
 *   * @n: Int to be operated upon
 *     * Return: Unsinged int value to calling function
 *      */
int _abs(int n)
{
		if (n > 0)
					return (n);

			else
					{
								n *= -1;
										return (n);
											}
}
