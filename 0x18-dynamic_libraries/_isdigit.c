#include "main.h"

/**
 *  * _isdigit - checks for digits
 *   * @c: The parameter to check
 *    * Return: 1 or 0
 *     */

int _isdigit(int c)
{
		if (c >= 48 && c < 58)
					return (1);

			else
						return (0);
}
