#include "main.h"
#include "main.h"

/**
 *  * _isupper - checks uppercase
 *   * @c: The parameter
 *    * Return: 0 in sucessful
 *     */

int _isupper(int c)
{
		if (c >= 65 && c < 91)
					return (1);

			else
						return (0);
}
